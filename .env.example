# Use the transaction pooler URL for runtime (Vercel/serverless).
# Format: postgresql://postgres.<project-ref>:YOUR_PASSWORD@aws-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1
DATABASE_URL=postgresql://postgres.<project-ref>:YOUR_PASSWORD@aws-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1

# Use the direct URL for migrations and local tooling.
# Format: postgresql://postgres:YOUR_PASSWORD@db.<project-ref>.supabase.co:5432/postgres
DIRECT_URL=postgresql://postgres:YOUR_PASSWORD@db.<project-ref>.supabase.co:5432/postgres
