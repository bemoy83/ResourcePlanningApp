import type { CSSProperties } from 'react';

export const stickySeparatorStyle: CSSProperties = {
  borderRight: 'var(--sticky-separator-width) solid var(--sticky-separator-color)',
  boxShadow: 'var(--sticky-separator-shadow)',
};
